# 历程拆解契约审阅报告（2026-07-22）

## 后续修复状态

本报告下方记录的是修复前事实。当前默认运行已切换到 `outputs/journey_workspace_20260610_new`：人工选帧、区域profile、区域扫描、事件复核和指标复核均由同一份 `journey_workspace.json` 解析。进一步校验发现原278条指标使用的是修改前profile，因此已按当前profile重新扫描；新指纹为 `8bf9dc8e...a700b`，结果仍为0条自动事件和278条指标。事件页当前只有1条人工高置信候选，历史16条事件不再进入默认服务。

统一XLSX/三图生成器、final gate和统一四页启动器均已实现。当前真实工作空间尚有1条事件与278条指标待人工复核，因此正式产物还未生成；这是人工门禁，不是契约断点。

## 结论

四个页面属于同一 Session，但审阅前不是同一条连续数据链：人工选帧和区域/指标属于当前 parallel v2 运行，功能事件页仍读取历史语义包。版本和来源此前没有总manifest说明，容易让使用者误以为“上一页的输出就是下一页当前展示的数据”。

## 当前运行事实

| 页面 | Session | 当前数据 | 状态 |
|---|---|---|---|
| 人工选帧 | `20260610_163422` | `manual_frame_review.json` 1.0 | 1条人工确认候选；旧runtime缺少真实 `sourceIndex/videoFile`，OCR适配尚不具备直接运行条件 |
| 区域校准 | 同上 | `ocr_region_profile` 1.1 | 15区：3确认、12排除；profile complete |
| 功能事件 | 同上 | 历史语义分支 | 16条已确认；语义输入已从1.0迁移为1.1，输入/候选/复核指纹一致 |
| 指标结果 | 同上 | 当前区域扫描 `metric_observations_v2.json` | 278条待复核，来源指纹 `351d021e...d80e` |

当前区域扫描的 `event_observations_v2.json` 为0条，因为所有事件区域均已人工排除。这不是版本错误，但说明事件页16条不是由这次区域扫描得到，而是历史人工事件分支。

## 已发现并修复

1. **v2事件候选没有进入事件页的适配器**：新增 `prepare_event_review_v2.py` 和 `event_review_manifest.json`，固定 v2候选到语义复核四件套的转换。
2. **运行中的语义输入1.0与仓库schema 1.1不一致**：增加显式迁移工具；当前24条源事件已迁移，16条人工确认结果保持不变。
3. **人工选帧会写猜测路径**：不再默认写不存在的索引/视频文件名；通过 `ocrHandoff.blockers` 报告缺口。
4. **清空人工选择后旧适配文件残留**：无拼图选择时同步删除 `selected_ocr_tiles.json`。
5. **工作台端口默认值与导航不一致**：功能事件统一8766，指标结果统一8769。
6. **最终产物入口可能重新带入排除/待复核事件**：确认语义文件进入产物准备时只保留 `semantic_review.status=confirmed`，并保留人工确认分类。
7. **版本关系没有唯一说明**：新增 `journey_contract_matrix.md`，规定生产者、消费者、版本、状态和来源指纹规则。

## 仍未完成

1. 当前人工选帧runtime来自历史目录，缺少真实 `sourceIndex` 和 `videoFile`；需要实现“从普通Session生成完整review runtime”的正式工具，才能让 `selected_ocr_tiles.json` 直接接OCR CLI。
2. 当前全量区域扫描没有可用事件区域，因此自动事件线为0条；功能事件仍依赖历史人工事件。这是候选策略/区域配置问题，不是契约问题。
3. 最终XLSX和三张正式图的统一生成器尚未完成。现有 `prepare_journey_analysis_v1.py` 仅生成草稿图和Agent交接包。

以上未完成项已在契约矩阵中明确标为边界，不再把它们描述成已贯通功能。
