# Screen Mouse Recorder Frontend PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | Screen Mouse Recorder |
| 文档类型 | 前端 PRD |
| 阶段 | MVP UI |
| 目标平台 | Windows 桌面 |
| 版本 | v0.2 |
| 更新日期 | 2026-07-01 |

## 2. 前端目标

本前端 PRD 定义 Screen Mouse Recorder MVP 的桌面界面体验。界面需要让用户在不理解技术细节的情况下完成一次录制：选择输出地址，打开记录选项，确认录制设置，开始录制，停止录制，并查看本次 session 输出位置。

MVP 前端重点不是做复杂编辑器，而是提供一个安静、可靠、可扫描的录制控制台。用户打开应用后，应能在 1 分钟内完成设置并开始录制。

## 3. 核心用户流程

1. 用户打开应用。
2. 用户查看当前录制区域状态。
3. 用户选择或确认输出地址。
4. 用户打开记录选项并确认需要记录的内容。
5. 用户点击开始录制按钮。
6. 应用进入录制中状态，显示计时、输出目录和停止按钮。
7. 用户停止录制。
8. 应用展示 session 输出位置和基础统计入口。

## 4. 信息架构

### 4.1 主界面分区

| 区域 | 说明 |
| --- | --- |
| 顶部状态栏 | 显示应用名称、录制状态、环境状态 |
| 录制控制区 | 放置开始录制按钮、停止按钮、计时器、区域状态 |
| 输出地址区 | 显示当前输出根目录，支持选择目录和打开目录 |
| 记录选项区 | 默认展开，提供记录范围和采样选项 |
| Session 结果区 | 停止录制后展示最近一次输出文件夹和摘要 |

### 4.2 首屏要求

首屏必须直接呈现可操作的录制控制台，不做欢迎页、营销页或说明页。用户一进入界面就应看到：

- 开始录制按钮。
- 输出地址选择入口。
- 已打开的记录选项区域。
- 当前状态。

## 5. 页面布局方案

### 5.1 布局方向

采用紧凑型桌面控制台布局，视觉上接近专业工具面板。窗口建议默认尺寸为 `840 x 560`，最小尺寸不低于 `760 x 500`。

整体布局：

- 顶部一行：应用名、状态胶囊、环境检查结果。
- 中部左侧：录制主控制，包括区域、计时、开始/停止。
- 中部右侧：输出地址与记录选项。
- 底部：最近 session 输出结果和日志提示。

### 5.2 视觉风格

风格关键词：克制、清晰、工作台、可信赖。

建议视觉语言：

- 背景使用浅灰工作台底色，避免纯白刺眼。
- 主操作按钮使用高对比红色或珊瑚红，表达录制动作。
- 辅助按钮使用中性灰或低饱和蓝绿。
- 卡片圆角不超过 8px。
- 信息密度适中，优先保证录制时状态可扫读。
- 不使用大幅 hero、装饰性插画或营销式排版。

## 6. 核心功能需求

### 6.1 开始录制按钮

#### 需求描述

开始录制按钮是界面主操作。用户完成必要配置后，点击该按钮开始创建 session、启动鼠标记录和视频录制。

#### 显示规则

| 状态 | 按钮文案 | 可点击 | 说明 |
| --- | --- | --- | --- |
| 未选择区域 | 开始录制 | 否 | 提示先选择录制区域 |
| 未确认记录提示 | 开始录制 | 否 | 提示确认记录选项 |
| FFmpeg 不可用 | 开始录制 | 否 | 显示环境错误 |
| 可录制 | 开始录制 | 是 | 点击后进入启动中 |
| 启动中 | 正在启动 | 否 | 防止重复点击 |
| 录制中 | 停止录制 | 是 | 主按钮切换为停止动作 |

#### 交互要求

- 按钮应始终位于首屏显眼位置。
- 可录制时按钮为主视觉焦点。
- 点击开始后立即给出启动中反馈。
- 启动失败时恢复可操作状态，并展示失败原因。
- 录制中按钮切换为停止录制，避免同时出现两个强主按钮。

#### 验收标准

- 用户能明确识别当前是否可以开始录制。
- 点击开始后不会重复创建多个 session。
- 启动失败不会让界面卡死。

### 6.2 输出地址选择

#### 需求描述

用户可以在开始录制前选择输出根目录。每次录制会在该目录下自动创建唯一 session 文件夹。

#### 组件要求

| 元素 | 类型 | 说明 |
| --- | --- | --- |
| 输出路径展示 | 只读文本框 | 显示当前输出根目录 |
| 选择按钮 | 按钮 | 打开系统目录选择器 |
| 打开按钮 | 图标按钮或普通按钮 | 打开当前输出目录 |
| 空间状态 | 文本状态 | 可选显示路径是否可写 |

#### 交互要求

- 默认输出目录为 `sessions/`。
- 用户选择目录后立即更新当前配置。
- 录制中禁止切换输出目录。
- 输出目录不可写时，开始录制按钮不可点击。
- 停止录制后，结果区应显示本次 session 的完整路径。

#### 验收标准

- 用户能选择任意可写目录作为输出根目录。
- 路径变更会影响下一次录制。
- 录制结束后能一键打开输出目录。

### 6.3 记录选项打开

#### 需求描述

记录选项用于控制本次录制要保存哪些鼠标活动数据。MVP 阶段该区域默认打开，避免用户忽略隐私和数据范围。

#### 默认展示

记录选项区默认展开，并展示以下选项：

| 选项 | 默认值 | 控件 | 说明 |
| --- | --- | --- | --- |
| 记录区域外鼠标活动 | 开 | Toggle | 关闭后只记录录制区域内事件 |
| 鼠标轨迹采样 | 开 | Toggle | 开启后生成 `mouse_samples.jsonl` |
| 点击事件识别 | 开 | Toggle | 记录 down/up/click |
| 滚轮事件记录 | 开 | Toggle | 记录 wheel |
| 拖拽事件识别 | 开 | Toggle | 记录 drag_start/drag_move/drag_end |
| 同步标记 | 开 | Toggle | 开始录制时写入 sync_marker |

#### 高级选项

高级选项默认折叠，用户可展开调整：

| 选项 | 默认值 | 控件 | 说明 |
| --- | --- | --- | --- |
| 视频帧率 | 30 FPS | Stepper 或下拉 | 录屏帧率 |
| 鼠标采样率 | 30 Hz | Stepper 或下拉 | 轨迹采样频率 |
| 点击最大间隔 | 500 ms | 数字输入 | down/up 合成 click 的时间阈值 |
| 点击最大距离 | 8 px | 数字输入 | down/up 合成 click 的距离阈值 |
| 拖拽最小距离 | 10 px | 数字输入 | 判定 drag 的距离阈值 |

#### 隐私提示

记录选项区底部需要有一个录制确认勾选项：

```text
我已确认：应用将录制选定屏幕区域，并在本地记录鼠标活动数据。
```

未勾选前，开始录制按钮不可点击。

#### 验收标准

- 记录选项默认处于打开状态。
- 用户能在开始录制前关闭区域外鼠标记录。
- 用户能明确看到当前会记录哪些数据。
- 隐私确认未勾选时不能开始录制。

## 7. 关键状态设计

| 状态 | UI 表现 |
| --- | --- |
| Ready | 显示待录制，开始按钮根据配置决定是否可用 |
| Region Missing | 区域状态显示未选择，提示先选择区域 |
| Output Invalid | 输出地址区显示错误，开始按钮禁用 |
| Starting | 开始按钮显示正在启动，其他设置禁用 |
| Recording | 显示红色录制状态、计时器、停止录制按钮 |
| Stopping | 显示正在保存，禁止关闭关键流程 |
| Complete | 显示最近 session 路径、摘要和打开目录按钮 |
| Failed | 显示失败原因和可恢复操作 |

## 8. 数据绑定需求

前端需要读取并写入以下配置：

```json
{
  "video_fps": 30,
  "sample_fps": 30,
  "output_root": "sessions",
  "record_outside_region": true,
  "show_sync_marker": true,
  "click_max_duration_ms": 500,
  "click_max_distance_px": 8,
  "drag_min_distance_px": 10,
  "double_click_window_ms": 500,
  "ffmpeg_path": "..."
}
```

录制结束后前端需要展示以下结果：

- session 目录。
- `recording.mp4` 是否存在。
- 事件总数。
- 采样总数。
- 点击次数。
- 滚轮次数。
- 拖拽次数。

## 9. 可用性要求

- 所有核心操作必须在首屏可见。
- 录制中不能修改会影响 session 的设置。
- 错误提示要指出用户下一步该做什么。
- 路径文本过长时应中间截断，但 hover 或详情中可查看完整路径。
- 计时器每 100ms 到 250ms 更新一次即可。
- 停止录制后应清晰提示数据已保存。

## 10. 验收标准

MVP 前端验收需要满足：

1. 首屏包含开始录制按钮。
2. 首屏包含输出地址选择入口。
3. 记录选项区默认打开。
4. 用户能选择输出地址。
5. 用户能查看并调整记录选项。
6. 用户未确认隐私提示时不能开始录制。
7. 用户能开始和停止录制。
8. 录制中有明确状态和计时。
9. 录制结束后能看到 session 输出地址。
10. FFmpeg 缺失、输出目录不可写、区域未选择等错误能被前端明确展示。

## 11. 后续扩展

- 增加录制区域预览缩略图。
- 增加最近 session 列表。
- 增加数据文件快速打开入口。
- 增加配置预设，例如研究模式、隐私模式、轻量模式。
- 增加录制中悬浮控制条。
- 增加录制完成后的摘要图表。

## 12. 点击关键帧合成图 UI

### 12.1 功能定位

点击关键帧合成图是后续分析增强功能。它不应占用录制页主操作空间，录制页只展示生成状态和快速打开入口；详细参数放在分析处理页或独立合成图区域中。

### 12.2 页面入口

推荐入口：

- 录制页最近 Session 区：显示“关键帧图：已生成 / 无点击事件 / 生成失败”。
- 分析处理页或合成图页：提供重新生成入口。
- Session 输出目录：生成后的 PNG 文件可直接打开。

### 12.3 控件要求

合成图区域提供以下控件：

| 控件 | 默认值 | 说明 |
| --- | --- | --- |
| 合成模式 | 点击关键帧 | 可与均匀抽帧并存 |
| 最大帧数 | 60 | 控制输出数量 |
| 每行张数 | 5 | 控制网格密度 |
| 时间去重 | 500ms | 合并短时间重复点击 |
| 距离去重 | 20px | 合并同区域重复点击 |
| 画面去重 | 开启 | 合并近似画面 |
| 显示点击点 | 开启 | 在帧上绘制点击位置 |
| 显示时间戳 | 开启 | 在帧上显示视频时间 |
| 包含拖拽事件 | 关闭 | 第一版默认关闭 |

控件应保持紧凑，避免把分析页做成复杂仪表盘。参数文案使用短标签，详细解释放 tooltip。

### 12.4 状态设计

| 状态 | UI 表现 |
| --- | --- |
| 未生成 | 显示待生成，生成按钮可用 |
| 生成中 | 按钮禁用，显示进度或“生成中” |
| 已生成 | 显示文件名和打开输出按钮 |
| 无点击事件 | 显示“无点击事件”，不作为错误 |
| 缺少视频 | 显示缺少 `recording.mp4` |
| 缺少鼠标日志 | 显示缺少 `mouse_events.jsonl`，可提示均匀抽帧兜底 |
| 生成失败 | 显示失败原因和重试入口 |

### 12.5 图像视觉规则

- 小图保持统一尺寸，避免合成图布局跳动。
- 每个小图显示序号和时间戳。
- 点击点使用清晰圆点或圆环，不遮挡画面主体。
- 文字不应溢出小图边界。
- 多页输出时文件名和状态应能对应到具体页。

### 12.6 验收标准

1. 用户能从分析处理页生成点击关键帧合成图。
2. 用户能调整最大帧数、每行张数、时间/距离去重等参数。
3. 生成过程中 UI 不冻结。
4. 生成完成后能打开输出目录。
5. 无点击事件、缺视频、缺日志时有明确状态，不弹出无意义错误。
6. 录制页最近 Session 区能看到关键帧图生成状态。
